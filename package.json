{
  "name": "flapjs",
  "version": "1.0.0",
  "description": "A Flappy Bird-style game implementation",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "test": "vitest run",
    "test:run": "vitest run",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "jsdom": "^23.0.0",
    "prettier": "^3.5.3",
    "vitest": "^1.0.0"
  }
}
